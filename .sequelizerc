// custom configuration for sequelize library

const path = require('path');

module.exports = {
  config: path.resolve('src/config', 'config.js'),
  'seeders-path': path.resolve('server/database/postgree', 'seeders'),
};
